<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.mybatis.mapper.chart">
<select id="chartSel" parameterType="String" resultType="map">
select board_title, blist_title 
from board, board_list 
where board.board_no = board_list.board_no and board.board_no = #{board_no}
</select>
</mapper>